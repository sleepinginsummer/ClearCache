{
  "name": "clear-cache-extension",
  "version": "1.0.5",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check popup.js && node --check lib/core.js"
  },
  "engines": {
    "node": ">=18"
  }
}
